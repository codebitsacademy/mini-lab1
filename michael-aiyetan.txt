Question: What is the difference between an inline element and a block element?
Answer:Block-level Elements
Question:A block-level element always starts on a new line and takes up the full width available (stretches out to the left and right as far as it can).
Answer:Inline Elements
An inline element does not start on a new line and only takes up as much width as necessary.
What happens when an element is positioned absolutely?
The elements becomes fixed
How do I make an element take up only the amount of space it needs but also have the ability to give it a width?
padding
Name 4 elements that are diplay block by default, 5 elements that are display inline by default and 1 element that is display inline-block by default 5. In your own words, explain the box model. What is the fix for the box model?
Block by default: <p> <h1> <h2> <h3>
Element that are display inline by default: <button> <textarea> <input> <select> <img>
element that is display inline-block by default: span
What is Box model?
The CSS box model is essentially a box that wraps around every HTML element
What is the fix for the box model?
 It’s called box-sizing: border-box